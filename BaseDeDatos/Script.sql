USE generationg1;

/*SELECT * FROM customers c;

SELECT *
FROM customers c
WHERE c.customerName LIKE '%toy%';

SELECT c.customerNumber, c.customerName
FROM customers c
WHERE c.customerName LIKE '%toy%';

SELECT c.customerNumber, c.customerName, c.city
FROM customers c
WHERE c.city = 'Madrid';

SELECT c.customerNumber, o.orderNumber, c.customerName
FROM customers c, orders o
WHERE c.customerNumber = 496
ORDER BY c.customerNumber desc;


SELECT * from products p;

SELECT * from productlines pl;

SELECT p.productlines, pl.productlines, pl.textDescription
FROM products p, productlines pl
WHERE p.productline = pl.productLine
AND pl.producline = 'Motorcycles';*/

/*select quantityInStock  ,productName , p.productLine
from products p
where p.productLine = 'Motorcycles'
order by p.quantityInStock desc;


titlessalariestitlesSELECT p.productName, p.productLine, p.quantityInStock
FROM products p
WHERE p.productName LIKE '%ford%'
ORDER BY p.quantityInStock DESC;*/
